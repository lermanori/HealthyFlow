# Task #67

- Issue: #67
- PR: pending

Exposed the existing per-user calorie item history through an authenticated backend route so recent and most-used items are now fetched from server data instead of depending on browser-local state. Added a quick-insert section on the Calories page that lets the user prefill the entry form from that persisted history and updates automatically after logging food. When a user has no saved history yet, the page now shows a clear empty state and leaves the full entry form available as the fallback path.
