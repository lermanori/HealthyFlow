#!/usr/bin/env bash
# .githooks/post-commit
#
# Ledger entries are written by the AI agent before committing (see CLAUDE.md).
# This hook is intentionally a no-op — kept so the hooksPath config stays valid.
