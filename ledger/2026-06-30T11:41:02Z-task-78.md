# Task #78

- Issue: #78
- PR: pending

Added a Playwright regression around scheduled timeline cards so the compact hover state is exercised the same way a real user toggles the done checkbox. The test verifies that hovering no longer introduces the old scale transform and that the checkbox can be toggled on, off, and on again without missed clicks. Added an explicit checkbox aria-label to keep the interaction selector stable and accessible.
