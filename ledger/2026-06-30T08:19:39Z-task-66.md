# Task #66

- Issue: #66
- PR: pending

Implemented a calorie quick-insert add flow that now opens inside the add-entry modal instead of living as a passive chip strip on the page. The modal exposes `Recent` and `Most Used` tabs backed by the existing calorie-item API, supports in-tab filtering, and lets the user select an item with either the mouse or keyboard to prefill the intake form. Added a Playwright regression that verifies tab ordering, search/filter behavior, and keyboard selection against the real `/calories` page.
